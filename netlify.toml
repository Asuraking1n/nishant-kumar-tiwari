[build]
  command = "pnpm run build"
  publish = "build/client"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — any unknown path should return index.html so client routing can handle it.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
